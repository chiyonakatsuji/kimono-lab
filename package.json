{
  "name": "kimono-lab",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "node --env-file-if-exists=.env scripts/sync-instagram.mjs",
    "sync:test": "node scripts/sync-instagram.test.mjs",
    "sync:e2e": "node scripts/sync-instagram.e2e.mjs",
    "build:synced": "npm run sync && astro build"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.0",
    "tailwindcss": "^4.3.3"
  }
}
